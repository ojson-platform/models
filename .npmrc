# This package is installed on its own in GitHub Actions. A semver dependency
# on @ojson/infra must stay a registry package in pnpm-lock.yaml. Inside the
# metarepo, pnpm would otherwise rewrite it to a link into devops/infra.
link-workspace-packages=false

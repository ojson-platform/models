# Tasks

- [ ] `request-identity` — Scenario: Один ответ содержит прежний ключ и props; Scenario: Пропуск свойства и undefined — один ответ; Scenario: Вложенный undefined не входит в ответ; Scenario: null, false, ноль и пустая строка остаются в ответе; Scenario: Обращение и предустановка без имени отвергаются; Scenario: Без props мемоизация передаёт пустой объект; Scenario: Предустановка без props находится таким же обращением
- [ ] `cache-identity` — Scenario: Кэш не даёт идентичность Model без имени; Scenario: Пустое имя не получает идентичность; Scenario: Кэш без props берёт ключ пустого объекта
